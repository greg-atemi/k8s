apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oldmutual-pvc
  namespace: oldmutual
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
